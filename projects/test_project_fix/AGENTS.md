# Project Documentation

## Overview
This document provides information about the project structure and conventions.

## Workspace Information
- **Workspace Directory**: /tmp/test_project_fix
- **Project Type**: General development project

## File Operations
All file operations in this project must be accomplished using bash commands:
- Use `ls -la` for listing files
- Use `cat > filename << 'EOF'` for creating/modifying files
- Always verify file creation with `ls -la <filename>`

## Documentation Conventions
- All tasks must be completed using bash commands wrapped in code blocks
- No direct file writing - always use bash command methods
- After any file operation, verify existence and optionally show contents

## Commands Reference