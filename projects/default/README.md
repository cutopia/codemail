# Project Documentation

PROJECT CONTEXT:
Workspace directory: /home/dev/opencodeprojects/codemail/projects/default
CRITICAL: ALL tasks MUST be accomplished using bash commands in this directory.
For listing files, use 'ls -la'.
For creating/modifying files, use 'cat > filename' or similar bash commands.
After any file operation, verify it exists with 'ls -la <filename>' and optionally show its contents with 'cat <filename>'.
Wrap ALL bash commands in